{
  "name": "zan-docs",
  "version": "0.0.1",
  "description": "Docs for ZAN.TOP",
  "scripts": {
    "start": "npm run dev",
    "dev": "dumi dev",
    "build": "dumi build",
    "prepare": "husky install && dumi setup"
  },
  "authors": [],
  "license": "MIT",
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional"
    ]
  },
  "lint-staged": {
    "*.{md,json}": [
      "prettier --write --no-error-on-unmatched-pattern"
    ]
  },
  "devDependencies": {
    "@commitlint/cli": "^17.1.2",
    "@commitlint/config-conventional": "^17.1.0",
    "dumi": "^2.1.22",
    "dumi-theme-chakra": "^0.0.15",
    "husky": "^8.0.1",
    "lint-staged": "^13.0.3",
    "nprogress": "^0.2.0",
    "prettier": "^2.7.1"
  }
}
