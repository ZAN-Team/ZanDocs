{
  "name": "zan-docs",
  "version": "0.0.1",
  "description": "Docs for ZAN.TOP",
  "scripts": {
    "start": "npm run dev",
    "dev": "dumi dev",
    "build": "dumi build",
    "prepare": "husky install && dumi setup"
  },
  "authors": [],
  "license": "MIT",
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional"
    ]
  },
  "lint-staged": {
    "*.{md,json}": [
      "prettier --write --no-error-on-unmatched-pattern"
    ]
  },
  "devDependencies": {
    "@commitlint/cli": "^17.6.3",
    "@commitlint/config-conventional": "^17.6.3",
    "dumi": "^2.2.0",
    "dumi-theme-chakra": "^0.0.17",
    "husky": "^8.0.3",
    "lint-staged": "^13.2.2",
    "nprogress": "^0.2.0",
    "prettier": "^2.8.8"
  }
}
